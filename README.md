# soc-backdoor-detection
Linux backdoor detector for security operations centers (SOC). Performs deep system inspection to identify suspicious files, processes, network connections, cron jobs, and systemd services that may indicate compromise.
